# Override for building the app image locally from source instead of pulling
# the CI-published image.
#
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up --build
services:
  app:
    build: .
