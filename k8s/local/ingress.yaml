apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mansaoferta-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mansaoferta-service
            port:
              number: 80
